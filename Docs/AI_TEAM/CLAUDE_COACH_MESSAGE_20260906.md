# ข้อความถึงลูน่า — แนะนำตัวใหม่ + แผนงานที่เสนอ (2026-09-06)

สวัสดีครับลูน่า

ตอนนี้พี่ (project owner) เปลี่ยนบทบาทของผมใหม่ครับ ขอแนะนำตัวอีกครั้ง:

**ผมไม่ใช่ "ลุงหมาย" (Visual/Art specialist) อีกต่อไปแล้ว** งานศิลป์/Blender/การแตะ Unity โดยตรงทั้งหมด **ยุติแล้ว ณ ตอนนี้**

**บทบาทใหม่ของผม: โค้ช/ที่ปรึกษาให้คุณเท่านั้น** — ผมจะไม่ลงมือทำงานเองอีกต่อไป (ไม่แตะ Unity, ไม่แตะ Blender, ไม่รันโค้ด, ไม่แก้ไฟล์โปรเจกต์โดยตรง) เพื่อรักษาโควต้าการใช้งานของตัวเองไว้ และให้คุณเป็น **ผู้ลงมือทำเพียงคนเดียว** ตามกฎ "One Unity Executor" ที่คุณตั้งไว้แต่แรกอยู่แล้ว

หน้าที่ของผมจากนี้คือ: อ่านสถานะโปรเจกต์, วิเคราะห์, และ**บรีฟงาน**ให้คุณพิจารณา — ข้อเสนอทั้งหมดถือเป็น "คำแนะนำ" ไม่ใช่คำสั่ง คุณตัดสินใจเองว่าจะรับหรือปรับตามหลัก **Authority > Evidence > Specificity > Recency** ที่คุณวางไว้

---

## อัปเดตล่าสุดที่ผมทำเสร็จก่อนเปลี่ยนบทบาท

**UPM IPC Fix ได้รับการ runtime-verified แล้ว** ตามเงื่อนไขที่คุณกำหนด:
- รีบูตเครื่อง ปิด Unity ทุกตัวหมดแล้ว
- รัน `Docs/Tools/Unity_Batch_Safe.ps1` จริง เห็น `Connected to IPC stream "Upm-4660" after 0.6 seconds` ไม่มี `Could not connect to IPC stream` / `Received undefined` เลย
- รายละเอียดเต็มอยู่ที่ `Docs/UnityBatchLogs/UnityBatch_20260906_171337.log` และสรุปไว้ที่เอกสารที่ผมส่งให้พี่ก่อนหน้านี้

ปรับ default timeout ในสคริปต์เป็น 600s แล้ว (รอบ cold-reimport ใช้จริง 309s)

**→ สถานะ: 🟢 Certified (runtime-verified)** — พร้อมให้คุณ/พี่ ตัดสินใจร่วมกันว่าจะอัปเดต `AI_WORK_PROTOCOL.md` เมื่อไหร่ (ผมจะไม่แก้เองฝ่ายเดียวตามที่คุณขอไว้)

---

## สิ่งที่ผมอยากบรีฟให้คุณพิจารณาทำต่อ (เรียงตามลำดับที่ผมเสนอ — คุณตัดสินใจเองว่าเห็นด้วยมั้ย)

1. **เคลียร์ M5 Authority Chain ให้จบก่อน** — `M5_AUTHORITY_CHAIN_STATUS.md` ระบุว่า M5.1–M5.6 ยังไม่ถูกประกาศ CERTIFIED แม้ roadmap หลักจะเขียนว่า certified แล้วก็ตาม อยากให้คุณยืนยันว่าอันไหนคือความจริงล่าสุด เพราะ Gameplay/Rules ทั้งหมดต่อยอดจากจุดนี้

2. **ปิด Phase 3 Table Visual ให้จบ** — validate ball/spot alignment กับ physics spawn, ซ่อม pocket/fascia geometry ตาม `YOLO_M7_4_SCENE_REPAIR_PLAN.md` (เดิมเป็นงานของผม แต่ตอนนี้ส่งต่อให้คุณทั้งหมด หรือพี่จะหา AI ตัวอื่นมาทำก็ได้)

3. **รัน Play Mode validation ของ `147VR_MainScene`** ก่อนตัดสินใจลบ `SampleScene` — ตอนนี้ยังไม่มีหลักฐานว่าผ่านจริง

4. **เดินหน้า Gameplay/Rules (Phase 2)** ต่อเมื่อข้อ 1 ชัดเจนแล้ว

5. **`Session_Preflight.ps1`** ที่คุณ approve ไว้ว่าทำ "ทีหลัง" — ถ้าคุณคิดว่าถึงเวลาแล้ว ผมยินดีร่างสเปก/บรีฟให้ แต่การเขียนโค้ดจริงและรันทดสอบ ขอให้เป็นหน้าที่คุณครับ

---

ผมพร้อมช่วยคิด วิเคราะห์ วางแผน หรือ code review ให้เสมอ — แต่การลงมือทำจริงในโปรเจกต์ ตอนนี้เป็นหน้าที่คุณคนเดียวครับลูน่า

— Claude (บทบาทใหม่: โค้ช)
